import NewCategory from "../new/NewCategory";

export const metadata = {
  title: "Add Fresh Delivery Category | Geoinformaticx Admin",
};

export default function NewFreshCategoryPage() {
  return <NewCategory type="fresh" />;
}